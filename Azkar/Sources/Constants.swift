import SwiftUI
import UIKit

enum Constants {
    static var windowSafeAreaInsets: EdgeInsets = EdgeInsets(top: 0, leading: 0, bottom: 0, trailing: 0)
    static let INSTAGRAM_STORIES_URL = URL(string: "instagram-stories://share")!
}
